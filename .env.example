# ================= 环境说明 =================
# 开发模式（npm run dev）自动加载 .env.development → 测试环境
# 测试构建（npm run build:test）自动加载 .env.test → 测试环境
# 生产构建（npm run build）自动加载 .env.production → 正式环境
#
# VITE_USE_MOCK：不设置或为 "true" 时，未接入的页面数据接口使用内置 mock；
# 接口逐个接入真实后端后，把对应环境里的该变量设为 "false"。
# 登录等已接入的认证接口不受此开关影响，始终走真实后端。
VITE_USE_MOCK=true

# 后端 API 基础地址（结尾不带斜杠），接口路径拼接在其后，例如 /open/login
# 测试环境：https://apitest.bigread.ai/teacher
# 正式环境：https://api.bigread.ai/teacher
VITE_API_BASE_URL=https://apitest.bigread.ai/teacher
